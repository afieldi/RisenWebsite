{
  "name": "bot",
  "version": "1.0.0",
  "description": "",
  "main": "app.py",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "python app.py --prod",
    "dev": "python app.py",
    "heroku": "git subtree push --prefix bot heroku master"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  }
}
