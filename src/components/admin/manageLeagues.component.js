import React, {Component} from 'react';
import { Container } from 'react-bootstrap';

export default class ManageLeagues extends Component {
    render() {
        return (
            <section>

            </section>
        );
    }
}